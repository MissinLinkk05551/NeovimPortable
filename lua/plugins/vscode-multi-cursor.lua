return
    {
        'vscode-neovim/vscode-multi-cursor.nvim',
         -- ... other settings
        opts = {
            default_mappings = true,  -- Keep default keymappings (recommended)
            no_selection = false     -- Create selections with cursors (like VSCode)
        }
    } 
